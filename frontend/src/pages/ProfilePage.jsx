import PlaceholderPage from '@/components/common/PlaceholderPage'

export default function ProfilePage() {
  return (
    <PlaceholderPage
      title="Profile"
      description="Name, photo, and language preference will be managed here once authentication is in place."
    />
  )
}
